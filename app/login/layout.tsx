export const metadata = {
  title: "Login — Nitisetu AI",
  description: "Sign in to your Nitisetu workspace.",
};

export default function LoginLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
